{
  "name": "front-beer-logger",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/front-beer-logger/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/front-beer-logger"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "front-beer-logger:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "front-beer-logger:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "front-beer-logger:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "front-beer-logger:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "front-beer-logger:build:development"
        },
        "production": {
          "buildTarget": "front-beer-logger:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/apps/front-beer-logger"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/front-beer-logger/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
